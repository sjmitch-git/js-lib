export const round = (n: number) => {
  return Math.round(n);
};

export const roundUp = (n: number) => {
  return Math.ceil(n);
};

export const roundDown = (n: number) => {
  return Math.floor(n);
};
